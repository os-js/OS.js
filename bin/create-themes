#!/bin/bash

for i in `find src/themes/*.less`; do
  if [[ ! "$i" == src/themes/_* ]]; then
    name=$(basename ${i%.*})
    echo -e "\033[1;34mBuilding theme '${name}'\033[0m"
    lessc $i dist/themes/${name}.css
    cp -Rf src/themes/$name dist/themes/$name
  fi
done

