const _api = require('./core/vfs.js');
const _api = require('./core/vfs.js');

module.exports.init = function(opts) {
  return {
    api: function(method, args, cb) {
    },

    vfs: function(method, args, cb) {
    }
  };
};

